version: "3.8"

services:
  echoai:
    build:
      context: .
      dockerfile: Dockerfile
    image: echoai:latest
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
    restart: unless-stopped
